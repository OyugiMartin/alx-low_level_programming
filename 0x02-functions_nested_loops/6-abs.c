#include "main.h"

/**
 * _abs - returns the absolute value of a digit
 *
 *  @n: the number for which to return the absolute
 *
 *  Return: the absolute value n
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
